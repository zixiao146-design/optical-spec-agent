# Gmsh Adapter Evidence Fixture

Offline `.geo` scaffold evidence for the registered `gmsh` adapter. These
fixtures do not run Gmsh, require no network, and do not claim
production-grade physical validation.
